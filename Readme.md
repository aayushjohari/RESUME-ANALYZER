# 🔍 AI Resume Analyzer & Job Match Dashboard

A Streamlit-based application to analyze resumes and match them with job descriptions.

## 🚀 Features (Planned)
- Resume upload (PDF)
- Job description analysis
- Skill matching


## ▶️ Run the App
```bash
pip install -r requirements.txt
streamlit run app.py